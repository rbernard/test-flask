CREATE TABLE Users (
    id int NOT NULL AUTO_INCREMENT,
    FirstName varchar(255),
    LastName varchar(255),
    PRIMARY KEY (id)
);

CREATE TABLE Words (
    id int NOT NULL AUTO_INCREMENT,
    Word varchar(255),
    PRIMARY KEY (id)
);

